@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Er is een error opgetreden.</h2>

@if (Model.ShowRequestId)
{
    <p>
        ID: <code>@Model.RequestId</code>
    </p>
}
